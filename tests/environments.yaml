Environments:
  TestDB:
    name: TestDB
    type: sqlite
    path: plain_sql/TestDB.sqlite
  FinanceDB:
    name: FinanceDB
    type: postgres
    host: localhost
    port: 5432
    user: postgres
    password: postgres
    database: finance
DataLogger:
  TestDB:
    environment: TestDB
    schema: dq_reports
    create_table: True
  LocalFile:
    folder: tmp

